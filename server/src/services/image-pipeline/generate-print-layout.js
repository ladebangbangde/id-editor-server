const { FOLDERS } = require('../../constants/file'); const { buildFileName, absoluteUploadPath, relativeUploadPath } = require('../../utils/file-helper'); const printLayoutService = require('../ai/print-layout.service'); module.exports=async (hdPath, layoutType='eight')=>{ const count = layoutType==='six'?6:layoutType==='twelve'?12:8; const fileName=buildFileName('.jpg'); const out=absoluteUploadPath(FOLDERS.PRINT,fileName); await printLayoutService.generateLayout(hdPath,out,count); return { printPath: out, printUrl: relativeUploadPath(FOLDERS.PRINT,fileName) }; };
