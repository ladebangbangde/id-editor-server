const r=require('express').Router(); const c=require('../controllers/image.controller'); r.post('/generate',c.generate); r.get('/history',c.history); r.get('/:imageId/detail',c.detail); module.exports=r;
