const r=require('express').Router(); const c=require('../controllers/upload.controller'); const upload=require('../middlewares/upload.middleware'); r.post('/',upload.single('file'),c.upload); module.exports=r;
